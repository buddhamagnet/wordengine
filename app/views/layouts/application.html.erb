<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "wordengine" %></title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to "word engine", root_path %></a></h1>
        </li>
       <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
        <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
      </ul>
      <section class="top-bar-section">
      <!-- Right Nav Section -->
        <ul class="right">
          <%= render 'layouts/user_state' %>
          <!--<li class="active"><a href="#">emit</a></li>-->
          <li class="has-dropdown">
            <a href="#">engine</a>
            <ul class="dropdown">
              <li><a href="#">eat me</a></li>
              <li><a href="#">drink me</a></li>
              <li><a href="#">word lab</a></li>
            </ul>
          </li>
        </ul>
        <!-- Left Nav Section -->
        <ul class="left">
          <li><%= link_to 'about', page_path('about') %></li>
          <li><%= link_to 'stories', stories_path %></li>
          <li><%= link_to 'writers', users_path %></li>
        </ul>
      </section>
    </nav>
    <%= render 'layouts/messages' %>
    <div class="container">
      <%= yield %>
      <%= javascript_include_tag "application" %>
    </div>
  </body>
</html>